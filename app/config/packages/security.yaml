security:
    # https://symfony.com/doc/current/security.html#registering-the-user-hashing-passwords
    password_hashers:
        Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface: 'auto'

    # https://symfony.com/doc/current/security.html#loading-the-user-the-user-provider
    providers:
        utilizatori:
            entity:
                class: App\Entity\Utilizator
                property: email

    firewalls:
        dev:
            # Ensure dev tools and static assets are always allowed
            pattern: ^/(_profiler|_wdt|assets|build)/
            security: false
        main:
            lazy: true
            provider: utilizatori
            user_checker: App\Security\VerificatorUtilizator
            entry_point: App\Security\PunctIntrareApiV1
            json_login:
                check_path: api_v1_autentificare
                username_path: email
                password_path: parola
                success_handler: App\Security\SuccesAutentificareHandler
                failure_handler: App\Security\EsecAutentificareHandler
            login_throttling:
                max_attempts: 5
                interval: '15 minutes'
                cache_pool: cache.autentificare_limiter
            logout:
                path: api_v1_deconectare
                invalidate_session: true
                enable_csrf: false

    # Note: Only the *first* matching rule is applied
    access_control:
        - { path: ^/api/v1/stare$, methods: [GET], roles: PUBLIC_ACCESS }
        - { path: ^/api/v1/token-csrf$, methods: [GET], roles: PUBLIC_ACCESS }
        - { path: ^/api/v1/autentificare$, methods: [POST], roles: PUBLIC_ACCESS }
        # Rutele exista exclusiv in mediul test pentru infrastructura API v1.
        - { path: ^/api/v1/test/(exceptie-interna|prea-multe-cereri|log-corelare)$, roles: PUBLIC_ACCESS }
        - { route: ^api_v1_, roles: IS_AUTHENTICATED_FULLY }

when@test:
    security:
        password_hashers:
            # Password hashers are resource-intensive by design to ensure security.
            # In tests, it's safe to reduce their cost to improve performance.
            Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface:
                algorithm: auto
                cost: 4 # Lowest possible value for bcrypt
                time_cost: 3 # Lowest possible value for argon
                memory_cost: 10 # Lowest possible value for argon
        firewalls:
            main:
                login_throttling:
                    max_attempts: 2
                    interval: '15 minutes'
