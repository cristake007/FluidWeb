---
# Dev Container-specific overrides: enable XDebug and expose the full repository.
services:
  php:
    environment:
      XDEBUG_MODE: "${XDEBUG_MODE:-develop,debug}"
      XDEBUG_CONFIG: "client_host=127.0.0.1 start_with_request=yes"
    volumes:
      - ./:/workspace:cached
